[package]
name = "veilysocial_backbone"
version = "0.1.0"
edition = "2021"
description = "Reusable probabilistic social-discovery backbone for VeilKnit applications"
license = "MIT OR Apache-2.0"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
